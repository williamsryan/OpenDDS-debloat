// this feature is used to turn on coverage instrumentation (for gcov)
// switched to default on. Always want coverage for PRAT.

feature(dds_non_coverage) {

  verbatim(gnuace,local) {

    CCFLAGS += --coverage
    LDFLAGS += --coverage

  }

}
